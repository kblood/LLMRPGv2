{
  "name": "@llmrpg/core",
  "version": "0.0.1",
  "description": "Core game engine and domain logic for LLMRPGv2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "eslint src",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
