{
  "name": "@llmrpg/llm",
  "version": "0.0.1",
  "description": "LLM adapters and context management for LLMRPGv2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@llmrpg/core": "workspace:*",
    "openai": "^4.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
